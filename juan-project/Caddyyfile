{
    # Escuchar puerto por variable de entorno PORT, o 80 si no está definido
    auto_https off
    http_port {$PORT}
}

:{$PORT} {
    root * /usr/share/caddy
    file_server
    encode gzip
    try_files {path} /index.html
}
